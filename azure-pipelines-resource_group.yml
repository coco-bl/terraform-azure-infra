trigger:
  branches:
    include:
      - main
  paths:
    include:
      - resource_group/**
      - modules/resource_group/**

pool:
  vmImage: 'ubuntu-latest'

variables:
  azureServiceConnection: 'website01'
  backendResourceGroup: 'tf-state-devrg01'
  backendStorageAccount: 'cotfbackenddev01'
  backendContainer: 'tfstate01'
  backendKey: 'resourcegroupsdev01.terraform.tfstate'

stages:
- stage: Deploy_Resource_Group
  displayName: 'Deploy Resource Group'
  jobs:
  - job: Terraform
    displayName: 'Terraform for Resource Group'
    steps:
    - task: TerraformInstaller@0
      displayName: 'Install Terraform'
      inputs:
        terraformVersion: 'latest'

    - task: TerraformTaskV4@0
      displayName: 'Terraform Init'
      inputs:
        provider: 'azurerm'
        command: 'init'
        backendServiceArm: $(azureServiceConnection)
        backendAzureRmResourceGroupName: $(backendResourceGroup)
        backendAzureRmStorageAccountName: $(backendStorageAccount)
        backendAzureRmContainerName: $(backendContainer)
        backendAzureRmKey: $(backendKey)
        workingDirectory: 'resource_group'

    - task: TerraformTaskV4@0
      displayName: 'Terraform Validate'
      inputs:
        provider: 'azurerm'
        command: 'validate'
        workingDirectory: 'resource_group'

    - task: TerraformTaskV4@0
      displayName: 'Terraform Plan'
      inputs:
        provider: 'azurerm'
        command: 'plan'
        workingDirectory: 'resource_group'

    - task: TerraformTaskV4@0
      displayName: 'Terraform Apply'
      inputs:
        provider: 'azurerm'
        command: 'apply'
        environmentServiceNameAzureRM: $(azureServiceConnection)
        workingDirectory: 'resource_group'
